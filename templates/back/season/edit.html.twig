{% extends 'base.html.twig' %}

{% block title %}Editer une saison{% endblock %}

{% block body %}
    <h1>Editer une saison</h1>

    {{ include('back/season/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a class="btn btn-primary mt-1" href="{{ path('app_back_season') }}">Retour à la liste</a>

    {{ include('back/season/_delete_form.html.twig') }}
{% endblock %}
